# TODO: ВЕРНУТЬ АВТОРИЗАЦИЮ ПОСЛЕ РАЗРАБОТКИ

## Когда возвращать
После того как ВСЯ функциональность будет работать:
- ✅ Список источников загружается
- ✅ Добавление источника работает
- ✅ Тест RSS работает
- ✅ Импорт статей работает
- ✅ Редактирование источника работает
- ✅ Удаление источника работает
- ✅ Нет ошибок в консоли
- ✅ Всё тестировано локально

## Что нужно будет сделать

### 1. Вернуть middleware проверки
Раскомментировать код в `middleware.ts`

### 2. Вернуть layout с сессией
Вернуть useEffect и проверку сессии в `app/adminko/sources/layout.tsx`

### 3. Вернуть API проверки
Раскомментировать `getSession()` проверки во всех:
- `app/api/admin/sources/route.ts`
- `app/api/admin/sources/test/route.ts`
- `app/api/admin/sources/[id]/route.ts`
- `app/api/admin/sources/[id]/import/route.ts`

### 4. Убрать страницу логина
Удалить `app/adminko/page.tsx` (логин)
Или оставить для продакшена

### 5. Протестировать
- Проверить что редирект работает
- Проверить что логин работает
- Проверить что logout работает
- Проверить что API требуют авторизацию

## Credentials
Username: boss
Password: 149521MkSF#u*V

## Файлы для восстановления
Можно найти в Git истории коммиты до отключения auth.
