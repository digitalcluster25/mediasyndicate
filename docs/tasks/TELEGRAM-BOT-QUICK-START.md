# 🤖 БЫСТРЫЙ СТАРТ: Создание Telegram бота

## ШАГ 1: СОЗДАНИЕ БОТА (2 минуты)

### 1.1 Открой Telegram
- Открой приложение Telegram на телефоне или компьютере
- В поиске найди: **@BotFather**

### 1.2 Создай нового бота
1. Отправь команду: `/newbot`
2. BotFather спросит имя бота → введи: `MediaSyndicate Bot`
3. BotFather спросит username → введи: `MediaSyndicateNewsBot` (или любой свободный)
4. **BotFather вернет токен!** Сохрани его.

**Пример токена:**
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz123456789
```

### 1.3 Сохрани токен
Скопируй токен - он понадобится для следующих шагов.

---

## ШАГ 2: ДОБАВЛЕНИЕ ТОКЕНА

### Вариант A: Через терминал
```bash
cd /Users/macbookpro/Desktop/mediasyndicate
echo "TELEGRAM_BOT_TOKEN=твой_токен_здесь" >> .env.local
```

### Вариант B: Вручную
1. Открой файл `.env.local` в корне проекта
2. Добавь строку:
```
TELEGRAM_BOT_TOKEN=твой_токен_здесь
```
3. Сохрани файл

---

## ШАГ 3: ТЕСТИРОВАНИЕ

### 3.1 Запусти dev server
```bash
npm run dev
```

### 3.2 Тест через скрипт (рекомендуется)
```bash
npx tsx scripts/test-telegram-token.ts
```

### 3.3 Тест через API
```bash
curl -X POST http://localhost:3000/api/admin/sources/test \
  -H "Content-Type: application/json" \
  -d '{"url": "@uniannet", "type": "TELEGRAM"}'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "itemsFound": 20,
  "sample": {
    "title": "...",
    "pubDate": "...",
    "link": "https://t.me/uniannet/12345"
  }
}
```

---

## ШАГ 4: ПОДПИСКА БОТА НА КАНАЛ

⚠️ **ВАЖНО:** Бот должен быть подписан на канал для получения сообщений!

### Как подписать бота:
1. Открой канал в Telegram (например, @uniannet)
2. Если ты администратор канала:
   - Настройки канала → Администраторы → Добавить администратора
   - Найди бота по username (например, @MediaSyndicateNewsBot)
   - Добавь как администратора (или просто участника)
3. Если ты не администратор:
   - Попроси администратора канала добавить бота

---

## ШАГ 5: PRODUCTION (Dokploy)

После успешного локального теста:
1. Сообщи токен
2. Попроси добавить `TELEGRAM_BOT_TOKEN` в Dokploy environment variables
3. После добавления - проверим production

---

## TROUBLESHOOTING

### Ошибка: "TELEGRAM_BOT_TOKEN not configured"
- Проверь что токен добавлен в `.env.local`
- Перезапусти dev server после добавления токена

### Ошибка: "Failed to get channel info"
- Проверь что канал существует и публичный
- Проверь формат: `@username` или `https://t.me/username`

### Ошибка: "No posts found"
- Бот должен быть подписан на канал
- Бот получает только новые сообщения (после подписки)
- Для истории нужен Telethon (Python)

---

## ГОТОВО!

После успешного теста - сообщи и я продолжу с production настройкой.

